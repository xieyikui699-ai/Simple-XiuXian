# 更新日志

本仓所有重要变更记录于此。格式参考 Keep a Changelog；版本号语义：引擎与小程序同步演进。

## [Unreleased]

### 主页「外门挖矿」滑杆卡片 + 挖矿单价 3 → 1 灵石/人/月

- **玩法**：主界面宗门管理卡片下方新增「外门挖矿」卡片——拖动滑杆投入外门弟子挖矿，每投入 1 人每月上缴 **1 灵石**（与供奉并存、参与资源危机判定，资源长老不加成）；滑杆上限 = 外门总数 − 丹房·器坊投入 − 练气在编（与 `assignOuterJobs` 同一容量口径），卡片实时显示「+N 灵石/月」与「已投入 N 人」。
- **引擎**：`OUTER_MINING_STONES_PER_MONTH` 3 → 1（原 WIP 口径 3 灵石按玩家需求改为 1）；结算公式/危机判定/旧档兼容不变，`outer-jobs.test.ts` 断言按常量引用同步。`balance-sim.mjs` 不含挖矿，调平快照不受影响。
- **小程序**：`main.tsx` 宗门管理卡片后插入「外门挖矿」卡片（Taro `Slider`，`onChange` 调 `store.assignOuterJobs({ mining, qi })`——引擎对未传键归零，qi 随行带回）；H5 桌面鼠标拖拽复用既有 `slider-mouse-bridge`。
- **验证**：引擎 183 项 + 小程序 7 项测试全绿；H5 浏览器实测拖动至 50 人，推进一月后「灵石 +359」= 供奉 200 + 挖矿 50（50×1）− 俸禄 40（4×10）+ 历练收获 149，与预期逐分吻合。
- **文档**：设计文档 §宗门体系/§灵石经济/§页面清单 同步，增变更记录（2026-09-08 七）；更正变更记录（2026-09-07 六）所称「弟子殿外门分工面板」未落地之误（该面板仍为规划项，挖矿入口以主页卡片为准）。

### 战斗属性派生改「等级 × 五维」耦合式（境界压制）+ 暴击改悟性驱动

- **公式**（`combat-profile.ts`，玩家与 NPC 共用）：最大生命 = 100 + 实力等级×体魄 + (实力等级−1)×50；物理攻击 = 30 + 实力等级×(力量/4) + (实力等级−1)×10；法术威力 = 30 + 实力等级×(魂力/4) + (实力等级−1)×10 + 法宝法威；防御 = ⌊5 + 实力等级×(体魄/4) + (实力等级−1)×4⌋ + 功法防御平加；**暴击率 = 10 + 悟性/4 + 天赋暴击（上限 50）**；先攻不变。**暴伤 1.5× → 2×**（`CRIT_MULTIPLIER`）。旧式（体魄×5 / 力量×2 / 魂力×2 / ⌊体魄×1.5⌋）攻防完全不吃境界，而五维终生不成长 → 境界只买到血量，跨境界压制几乎不存在、后期战斗血牛化；暴击与五维无关且暴伤偏低。
- **数值效果**：等级是乘区、五维是基数——境界压制为主、先天五维为辅；防御每级 +4 刻意慢于攻击 +10，保伤害不被稀释。均值弟子练气→元婴：生命 150→1050（七倍）、净伤 29→112、击杀回合 5.2→9.4 温和拉长；练气力量 90（攻击 53）打元婴防御 166 只剩最小伤害 1，境界碾压成立。悟性自此兼具修炼加速与战斗暴击（均值 23%、悟性 90 达 33%）双重作用。
- **小程序**：弟子详情/候选详情「战斗属性」卡由六行纵列改为两排网格（每排 3 项、标签在上数值在下，样式对齐五维卡）。
- **测试**：`combat.test.ts` 公式断言与 golden 注释改新数值（CD 法术单发 428→741、平局铁桶生命 1550、灼烧 tick 21→9、暴伤用例 ×1.5→×2、暴击统计口径 5%→23%）；1 级「刮痧」不再成立（攻击下限 30 > 减伤上限 24），状态抗性测试改 10 级构造（攻 123 对防 291 恒 min 1）；伤势测试强者升 10 级完全压制（旧 1 级互殴在暴击 23% + 生命 150 下偶有翻盘 seed）；灼烧致死测试改「10 级互压刮痧 + 业火灼烧磨死」构造（暴伤 2× 会让直伤抢在首枚 tick 前击杀）；`campaign.test.ts` 会战记录配对数放宽为 2–3（连续会战伤员多时「不足 3 人按实有数」合法）。引擎 183 项 + 小程序 11 项全绿。
- **模拟**：探针 15 局/档、上限 1200 月——easy/standard/hard 吞并 86.7%/40%/40%、被吞并 0%/0%/0%、玩家退出 2/9/9 局；等级差与被吞并锚点全过，「吞并 ≤25%」三档超标且较旧基线（46.7%/20.0%/40.0%）进一步升高（境界耦合放大滚雪球），重新定标为既有待办。

### 丹房/器坊重做：投入即自动开炉 + 长老境界加成 + 数值重定

- **玩法**：拖动滑杆投入外门弟子（≥1 人）该房即自动开工——无需点选任务、无需任命主持（丹房即炼丹；器坊默认法宝档 1，点选图纸换目标，换图纸进度作废）。每炉炼制点数 30 → **300 点**，满点出炉在延寿丹/聚灵丹中随机各半（`sha256(${seed}:pill-craft:${turn})` 掷骰口径不变）；点数基础 1 点/人/月不变，月耗降为 **1 灵石/人/月**（原丹房 13/器坊 10）。
- **主持（长老）加成改境界驱动**：丹师/工匠境界越高加成越高——每境界等级 +20%（`WORKSHOP_BONUS_PER_REALM_LEVEL`），元婴前期 +200% 封顶（`WORKSHOP_MAX_MASTER_BONUS_PCT`，`workshopMasterBonusPct` 取代原平尺 +20%）；丹道/器道天赋 +20% 与境界加成合计仍封顶 200%；无主持照常开工（无加成）。
- **小程序**：丹房·器坊页重排——任命/卸任按钮移入标题行靠右；删「在炉任务/共需 30 点/停摆：未投入外门弟子」文案，在炉仅剩进度条 +「进度 X/300 点 · 预计 N 月后出炉」（灵石不敷时显示停摆原因）；「投入外门弟子」行同排显示「N灵石/月」与最右「已投入 N 人」；丹方面板改两行小字（延寿丹 · 寿命+10年 / 聚灵丹 · 12月真元×1.5），器坊图纸点选即送炉。
- **测试**：`production.test.ts` 按新数值重写（投入即自动开炉、境界加成 +200% 封顶、20 人 15 月出炉节奏、元婴主持 +100 人当月出炉、1001 月分布长跑）；引擎 182 项 + 小程序 7 项测试全绿。
- **文档**：设计文档 §丹药系统/§装备 生产口径与 §页面清单 丹房行同步，增变更记录（2026-09-08 五）。

### 数值调平：境界真元阈值万进位重排 + 灵根倍率收敛

- **境界表（`realms.ts`）**：突破阈值改万进位节奏——练气 1,000/3,000/5,000、筑基 10,000/30,000/50,000、金丹 100,000/300,000/500,000（原 300–20,000 线性小步）；基础突破率同步下调（50/40/30/40/30/20/25/20/10）。元婴前期顶点语义落进表内：`requiredZhenyuan = Number.POSITIVE_INFINITY`、成功率 0（月结 `TOP_REALM_LEVEL` 守卫不触达，纯表达「无下一段」）。
- **灵根倍率（`roots.ts`）**：五行杂 ×1 / 四灵根 ×1.2 / 三灵根 ×1.5 / 双灵根 ×2 / 天灵根 ×3（原 1.5/2/3/5 档差过陡）；月度真元基准不动（`ZHENYUAN_BASE` 130 / 悟性系数 0.5），整体修行节奏显著放慢。
- **小程序**：顶点境界真元需求显示「∞」——`ui/display.ts` 新增 `formatZhenyuanRequirement`，弟子殿列表/共享选人组件/弟子详情三处接线。
- **测试**：`catalog.test.ts` 锁定新阈值/成功率全表与倍率表；`engine.test.ts` 相应断言改新值（修炼视图 1,000、突破失败率 50→55 递进、圆满止步用例改有限哨兵值）；`rival.test.ts` NPC 突破用例改「恰好达阈」构造。引擎 180 项 + 小程序 7 项测试全绿。
- **模拟口径**：`balance-sim.mjs` 去「超时」——游戏本体没有时间上限，收尾只有胜利（吞并）、失败（被吞并/凋敝）、玩家自己退出三种；月数上限到点未分胜负记为 `player_quit`（玩家退出），与引擎结局区分统计，不进锚点。
- **文档**：设计文档 §境界体系 表格与 §弟子 灵根倍率同步，增变更记录（2026-09-08 三）。千局模拟未重跑：新节奏下进度显著放慢——探针 15 局/档、上限 1200 月，easy/standard/hard 吞并 46.7%/20.0%/40.0%、被吞并 20.0%/13.3%/13.3%、未分胜负玩家退出 5/10/7 局，等级差锚点全过而「吞并/被吞并 ≤25%」在 easy/hard 超标，需重新定标；`scripts/balance-report.json` 仍为旧调平快照。

### 修复 H5 桌面端滑块无法拖动（鼠标→触摸桥）

- **问题**：Web/H5 验证基座中，丹房/器坊「投入外门弟子」滑杆鼠标拖不动——Taro 4.2.1 的 H5 Slider（`taro-slider-core`）只注册 `touchstart/move/end` 监听，无任何鼠标事件处理，桌面浏览器（含本仓 `web-serve.mjs` 基座）鼠标手势全程无响应；真机触屏不受影响。
- **小程序**：新增 `ui/slider-mouse-bridge.ts`——document 级 mousedown 命中 `.weui-slider__handler` 后，把手势翻译成合成 touch 事件派发给把手（move/up 挂 window，尊重 `disabled`），`app.ts` 以 `TARO_ENV === "h5"` 守卫安装（weapp 构建摇树移除）。
- **验证**：`build:h5` + 浏览器真实鼠标拖动——丹房滑杆 0 → 50、器坊 0 → 16，`store.setStaff` 落账（`jobs.*.assignedOuter`）、React 受控 value 回写均确认；weapp 端不引入任何行为变化。

### 突破顿悟（突破成功 20% 概率直接领悟未修法术）

- **玩法**：弟子突破成功时有 **20% 概率顿悟**——灵光乍现直接领悟一门未修法术（候选 = 12 门法术目录中该弟子未修者随机一门；已修满 2 门则不触发），不经藏经阁、不耗书。法术习得路径在「历练奇遇得书 → 藏经阁研读」之外新增「突破顿悟」一条；顿悟不与奇遇掉书联动（藏书不变）。NPC 宗门不参与（简化名册无法术体系）。
- **引擎**：月结第②步突破成功分支接顿悟掷骰（判定 `${seed}:epiphany:${discipleId}:${turn}` < `EPIPHANY_CHANCE_PCT`，选书 `:pick` 命名空间，确定性可对拍）；`BreakthroughEvent` 增可选字段 `epiphanySpellId`/`epiphanySpellName`（旧结果缺省兼容），编年史增里程碑条目「X 突破之际灵台清明，顿悟法术《Y》！」。
- **测试**：`engine.test.ts` 新增 3 项——扫描确定性 seed 断言顿悟触发（法术入 `spellIds`、纪事在案）、未命中不新增、已修满 2 门命中也不新增。
- **小程序**：主页月结卡片突破成功行追加「，顿悟《X》」；编年史页、弟子详情法术列表自动展示（读既有数据流）。
- **文档**：设计文档 §境界体系 增突破顿悟规则行与变更记录（2026-09-08 二），§法术目录 前言补路径注记；`catalog.ts` 头注同步。

### 移除飞升结局（胜负收敛为吞并单一）

- **玩法**：飞升胜利条件删除——元婴前期（境界 10 段尽头）改为修行圆满止步：圆满弟子不再积累真元、不再尝试突破（玩家与 NPC 同规），月结照常推进。胜利唯一路径为吞并：对方声望 ≤0 且我方宗门等级更高；失败两途不变（被吞并/宗门凋敝）。
- **引擎**：`EndingKind` 去 `ascension`、`BreakthroughOutcome` 去 `ascended`；月结突破循环对顶级弟子（`TOP_REALM_LEVEL`）整体跳过，NPC 月度推进同规（原 NPC 满级后反复空掷突破的循环一并修正）；`game_already_ended` 终局锁语义不变，测试覆盖迁至吞并结局用例。
- **测试**：`engine.test.ts` 飞升结局用例改「元婴前期圆满止步」行为断言（无结局、真元冻结、无突破记录）；`campaign.test.ts` 大境界声望 +3 用例改纯函数口径，评级与终局锁断言移入「三结局判定与评级」；引擎 176 项全绿。
- **小程序**：结局页删「飞升 · 仙途圆满」标签、进行中规则文案改吞并口径；`display.ts` 删「白日飞升」突破结果标签。
- **文档**：设计文档 §已确认决策/§境界体系/§胜负与结局评价/§月结流程/§数值调平 同步改口径并加变更记录（2026-09-08，调平章节标注飞升锚点为历史口径）；README 玩法简介与调平结论注记。`scripts/balance-sim.mjs` 去飞升统计与锚点。

### 外门恒满员（总数 = 等级上限 100/300/500），移除初始 20 与自然流动

- **玩法**：外门口径定稿——**外门总数恒等于当前宗门等级的外门上限**（1/2/3 级 = 100/300/500），开局即满员、升阶即扩容；主页「外门 X/Y」= **在岗/总数**（X = 丹房/器坊/挖矿/练气四类在岗合计，每分岗一人 +1，开局无人分岗即 0；Y = 100/300/500），弟子殿外门分工面板看总数与分工。外门自然流动（声望来投/士气离去）与 NPC 按频率招募外门机制移除——外门不再是「从 20 呼吸增长」的活名册；月结弹层删【来投】【离去】行。
- **规则**：1 级月供奉 40 → 200（2 级 600、3 级 1,000，资源长老 +20% 照加）；招募 300 灵石直入内门不变；坐化递补仍生成新内门弟子、外门总数不减；资源危机/凋敝在恒满员供奉下正常游玩不可达（判定代码保留备用）。
- **引擎**：删除 `GameState.outerCount`/`RivalSect.outerCount` 字段（总数按 `sectLimitsFor(rank).outerLimit` 派生）、`INITIAL_OUTER_DISCIPLES`/`RIVAL_INITIAL_OUTER_DISCIPLES`/`rivalRecruitPlan`；模块 `outer-flow.ts` 整体移除；月结 **9 步 → 8 步**（`MONTHLY_STEP_ORDER` 去 `outerFlow`），`SettlementResult` 删 `outerJoined`/`outerLeft`；旧档 `migrateGameState` 剥离遗留 `outerCount` 键（幂等）。
- **测试**：删除 `outer-flow.test.ts`；engine/rival/production/catalog/outer-jobs/expedition 断言改恒满员口径（危机用例改「内门膨胀俸禄 > 供奉」构造），引擎 176 项 + store 7 项全绿。
- **文档**：设计文档 §简化原则/§宗门体系/§灵石经济/§NPC 对手宗门/§月结流程（8 步）同步改口径，新增变更记录（2026-09-07 八）；锚点重跑结论见 §数值调平。

### 移除存档功能（状态仅存会话内存）

- **玩法**：按用户决策删除全部存档能力——主界面「手动存档（存入1/2/3格）」按钮组与首页「继续征程（存档列表/继续上局）」卡片移除；游戏进行中状态仅存于会话内存（globalThis 单例，跨页面共享），小程序冷启动/进程重启即回开局页重新开新局。
- **小程序**：删除 `store/save-adapter.ts`（存档适配层整体退役）；`game-store` 去掉 `SaveBackend` 注入与 `persistAuto`/`loadSlot`/`saveToSlot`/`deleteSlot`/`refreshSaves`（`Difficulty`/`DIFFICULTY_DISPLAY_NAMES` 迁入 game-store 导出）；`use-game` 首次建仓时一次性清除历史版本存档残留 key（`simple-xiuxian-save-1..4`、`simple-xiuxian-current-slot`）。
- **引擎**：零改动（`migrateGameState` 等旧档迁移设施保留在引擎内，UI 不再引用）。
- **测试/文档**：store 单测重写为纯内存口径 7 项全绿（去掉存档 round-trip/指针/体积/旧档迁移用例；研读/服丹正向路径由引擎测试覆盖）；README 存档规格章节改为「无存档策略」，设计文档 §工程方案/§页面清单同步收口。

### 丹房去丹方选择（炼制点数统一 30，出炉随机各半）

- **玩法**：丹房不再点选丹方——点「炼制」即送炉开工（挂任务免费），炼制点数统一 **30 点**；满点出炉时在延寿丹/聚灵丹中**随机（各 50% 概率）**入仓库并连炉续炼，重复点选「炼制」幂等不影响在炉进度。器坊图纸点选流程不变。
- **引擎**：`WorkshopTask` pill 变体去掉 `pillId`（旧档带 pillId 的在炉任务读时剥离、进度保留）；`PillTaskSpec` 改 `{ kind: "pill" }`；`Pill` 目录删 `craftPoints` 字段；新常量 `PILL_TASK_POINTS = 30`；出炉丹方掷骰命名空间 `${seed}:pill-craft:${turn}`（月结先推进回目再推生产，每月一掷、同 seed 确定性可对拍）。
- **小程序**：store `setPillTask()` 去参；丹房·器坊页丹方面板改单一「开炉炼丹 · 共需 30 点」入口，注明出炉随机（延寿丹/聚灵丹各半）。
- **测试**：引擎 189 项 + store 15 项全绿（新增出炉分布接近对半与同 seed 双跑一致性断言）。

### 主页外门口径定为 在岗/上限 + 弟子殿补外门分工面板

- **玩法**：主页「外门 X/Y」定为 **在岗/上限**——X = 丹房/器坊/挖矿/练气四类在岗合计（`workingOuterCountOf`，每分岗一人 +1，开局无人分岗即 0），Y = 外门上限（1/2/3 级 = 100/300/500），开局显示「外门 0/100」；外门总数（开局 20，随自然流动增减）与分工调整移至弟子殿。补齐缺失的弟子殿**外门分工面板**：外门总数/上限 + 在岗合计、丹房/器坊投入人数（丹房·器坊页入口）、**挖矿/练气步进器**（此前两岗无任何 UI 入口）、供奉人数；各岗合计 ≤ 外门总数，超编由按钮禁用前置拦截、引擎容量校验兜底。
- **规则/引擎**：数值与月结逻辑零变动，仅订正 `outer-jobs.ts` 注释口径（主页 X = 在岗合计、Y = 上限，总数归弟子殿分工面板）。
- **文档**：设计文档 §宗门体系·外门分工、§页面清单·弟子殿同步改口径，新增变更记录（2026-09-07 六）。

### 丹房/器坊改外门投入制（去开炉）

- **玩法**：丹房·器坊页删除「岗位点数池 + 开炉按钮」模型，改为参考完整版 crafting-v2 的**外门投入制**——点选丹方/图纸即送炉开工（挂任务免费、不预扣灵石，页面不再出现「开炉」），拖动**滑杆投入外门弟子**，人数越多炼制越快（每名每月 +1 点，完整版炼丹/炼器同值）；每房 1 名主持（丹师/工匠，无职成年内门、不战斗不修炼）产速 +20%，持丹道/器道天赋再 +20%；在炉任务实时显示进度条、每月点数、月耗与「预计 N 月后出炉（连炉续炼）」。
- **规则**：月结第 ④ 步按在册人数扣月耗（丹房 13 灵石/人/月 ≈ 原丹药费用÷点数、器坊 10 = 原装备费用÷点数，总成本与原数值表一致，只把「一次性炉费」摊成逐月人力消耗）；点数满自动出炉入仓库并**连炉**（进度清零续炼同任务，换丹方/图纸或抽走人手即打断，进度不丢失）；器坊月推进封顶总点数 60%（完整版口径，防人海秒出档 4），丹房不封顶（40 人延寿丹当月出炉）；无人或灵石不敷月耗该房当月停摆（不推进不扣费，UI 显示「停摆：未投入外门弟子 / 灵石不敷月耗」）；外门流动减员致超编时按「保丹房」钳制并落 warning 纪事；危机月生产随历练一并停摆。装备仍按宗门等级限档（1→档2 / 2→档3 / 3→档4）。
- **引擎**：`WorkshopState` 改 `{ workers（主持 ≤1）, assignedOuter, task }`（旧档 points/tasks 点数池结构读时归零迁移，主持保首位）；`Pill` 目录删 `craftCost`/`craftMonths`（周期由人数决定）；新命令 `setWorkshopStaff`（滑杆，两房+挖矿/练气合计 ≤ 外门总数）/`startWorkshopTask`（同任务幂等保进度、换任务进度作废落纪事）；新纯函数 `workshopPauseReason`/`estimateCraftMonths`/`workshopSpeedMultiplier` 供 UI 与月结共用同一推导。`production.test.ts` 全量重写（任命/投入/挂任务/月耗/加速/封顶/连炉/停摆/裁剪/旧档迁移/确定性）。
- **小程序**：丹房·器坊页重写为 滑杆交互（Taro Slider，上限随他房与挖矿/练气在编动态收窄）+ 丹方/图纸点选送炉；store 命令 `craftPill`/`craftGear` → `setStaff`/`setPillTask`/`setGearTask`。另修复 Taro H5 按页分包时 store 模块被复制进多个页面 chunk、子页拿到空会话立即弹回首页的缺陷（`getGameStore` 单例挂 `globalThis`，跨副本共享）。
- **调平**：标准档 200 局抽检（外门投入制 + 新模拟策略：聚灵丹挂炉、丹 15/器 10 人）：中位飞升 32.1 年 ∈ 30–60 ✅、吞并/被吞并 0% ✅、等级差 ≤1 占比 0.83 ✅；报告 `scripts/balance-report-standard-newprod.json`。

### 外门分工（挖矿/练气）与主页在岗显示

- **玩法**：外门弟子只是数字，但可分岗做工——弟子殿外门新增**分工面板**：挖矿（每人每月上缴 3 灵石）、练气（内门坐化递补时每人携 20 真元入门，纪事注明），加上丹房·器坊投入（滑杆见丹房·器坊页）与供奉（无岗），各岗合计 ≤ 外门总数；主界面「外门 X/Y」改为 **在岗/总数**（原为 总数/容量，容量移至弟子殿标题）。
- **规则**：`GameState` 增 `outerJobs`（旧档缺省 = 全员供奉，零配置行为与此前完全一致）；新引擎模块 `outer-jobs.ts`（`assignOuterJobs` 命令、`effectiveOuterJobsOf` 生效钳制、`workingOuterCountOf` 在岗口径）；外门自然流动减员后按 丹房→器坊→挖矿→练气 顺序钳制，配置保留、人数回涨自动恢复；`setWorkshopStaff` 容量校验与丹房·器坊页滑杆上限同步扣减挖矿/练气在编；挖矿收入随月结第①步入账并参与危机判定（资源长老只加成供奉）。
- **测试**：引擎新增 `outer-jobs.test.ts` 8 项（校验/纪事幂等/钳制/旧档兼容/车间容量联动/挖矿收入同 seed 对照/递补真元对照），引擎 187 项 + store 15 项全绿。
### 外门弟子只是一个数字（外门计数化）

- **玩法**：外门弟子不再有名有姓——弟子殿外门只显示「外门 X/Y」人数与流动说明（无个体名册、无详情页入口）；月结弹层来投/离去改显人数（【来投】N 名散修慕名入门 /【离去】N 名外门弟子离宗下山）。
- **规则**：`GameState`/`RivalSect` 增 `outerCount` 计数，`Disciple.role` 字段废除（名册全员内门）；旧档经 `migrateGameState` 幂等迁移（外门个体折算计数并移出名册，玩家与对手同口径）；外门自然流动改为计数结算（来投两掷不变，离去期望 = 人数 × 阈值% 整数必离 + 小数一次掷骰进位）；**招募 300 灵石候选 3 选 1 直接录入内门**（受内门上限约束，原「入外门 → 提拔」两步取消，`promoteToInner` 移除）；坐化递补改为自外门计数 −1 生成新内门弟子入册；`SettlementResult.outerJoined/outerLeft` 由名单改为人数。
- **NPC 对手**：对手宗门外门同样只记人数（初始 20、按难度付费月招 +1、自然流动同规则），内门名册/会战/宣战口径不变。
- **调平**：模拟玩家策略同步改写（招募直入内门按经济配比扩张、外门不再手招；宣战声望门槛 40 → 55——旧门槛下玩家升阶提前，压制局在 easy/standard 以 40%/32% 抢先收局），重跑 1,200 局锚点全过：飞升中位 30.8–31.5 年、吞并/被吞并 0%、等级差 ≤1 占比 0.75–0.93（结论见设计文档 §数值调平）。
- **测试**：`outer-flow.test.ts`/`engine.test.ts`/`rival.test.ts` 等改写为计数口径，store 新增旧档迁移测试全绿；车间开炉相关测试与页面随并行「外门投入制」车间改造另行收口。

### 外门弟子自然流动（不再是静态数字）

- **玩法**：外门人数随声望与士气呼吸——高声望吸引散修慕名来投（真实生成弟子入册、落「慕名来投」纪事，免费），士气低迷时外门弟子辞别山门（士气 ≤30 的离去月为 warning 纪事）；外门越接近满员来投越稀，危机月无人来投。来投/离去逐月在月结弹层展示（【来投】/【离去】行），主界面「外门 X/Y」随之逐月变动，弟子殿外门名册同步增减。
- **规则**：月结由 8 步并为 9 步（经济 → **外门流动** → 真元突破 → 生产 → 历练 → NPC → 会战 → 胜负 → 年度衰老）；来投掷骰阈值 = 声望 × 1.5 × (1 − 外门/容量)（上限 100、次掷减半），离去阈值按士气取 1（≥70）/ 2（31–69）/ 6（≤30）；经济按「流动前」名册计供奉，来投不扣灵石、招募 300 灵石 3 选 1 渠道不变。
- **NPC 对手**：同一流动规则作用于对手宗门（掷骰命名空间 `:rival`），与按难度付费招募并存。
- **引擎**：新增 `outer-flow.ts`（`settleOuterFlow`/`outerLeavePctFor` 纯函数）并导出；`SettlementResult` 增 `outerJoined`/`outerLeft`；快照体积约束 D-022 自 100KB 放宽至 128KB（外门名册随声望自然增长所致，仍远低于小程序 1MB 存储上限），战报字节预算不变。
- **测试与文档**：引擎 173 项（新增 `outer-flow.test.ts` 9 项）+ store 14 项全绿；12 月经济断言改为按「流动前」名册实测对账；千局抽检验证锚点不破（见设计文档 §数值调平）。

### 移除月度方针机制

- **玩法简化**：删除主界面「月度方针」四选一卡片与引擎 `MonthlyPolicy` 概念，月结由 9 步并为 8 步。宗门每月按固定基线自动运转：弟子闭关修炼（真元基础公式，无 +30% 方针加成）、历练每月自动进行（六事件掷骰不变；原历练方针的月度 −2 士气/+1 声望取消，事件自身奖惩保留）、休养的伤势恢复 −2 移除（伤势按回目自然到期）、经营宗门的额外供奉移除。玩家专注管理决策（招募/提拔/功法法术/丹药装备/长老/丹房器坊/宣战）。
- **资源危机语义**：危机 = 灵石 + 收入 − 俸禄 < 0，当月停摆（收支勾销、历练停摆、士气 −3）；月结弹层文案改为「灵石危机：本月俸禄未能支付！」。
- **引擎细节**：`settleMonthly(state, options?)` 不再接收方针参数；`state.policy`/`SettlementResult.policy` 字段与方针常量删除（旧档中的 `policy` 字段被容忍，存档版本仍为 1）；战报动作条目不再落 `spellId/hit/crit` 的 undefined 键（保证快照 JSON 往返与内存态一致）。
- **测试与调平**：引擎 163 项 + store 14 项测试同步改写全绿；调平脚本删除 `choosePolicy` 策略，重跑 1,200 局锚点（结论见设计文档 §数值调平）。

### 首页移除「天命种子」输入

- 开新局不再要求玩家填写天命种子：首页删除该输入框，种子一律自动随机生成（`zx-<时间戳36进制>`）；引擎 sha256 确定性种子体系与同 seed 对拍测试不受影响（种子转为内部实现细节）。

### UI 重设计：9:16 竖屏适配（Web/H5 为主，weapp 同源受益）

- **9:16 手机舞台（H5）**：`src/index.html` 注入竖屏舞台脚本——视口比 9:16 宽（桌面/横屏）时按高度定宽（≤520px）居中成手机画幅（墨色幕布 + 机身边框光影），手机竖屏则全屏铺满；rem 基准随舞台宽度缩放（750rpx = 舞台宽），任意窗口下保持手机端比例。
- **页面壳重构**：全局 `.page / .page-body / .page-footer` 竖屏三段壳——内容区独立滚动、主殿"推进一月 + 自动推进"固定底栏（含 safe-area）、H5 专属墨色返回栏 `BackBar`（`process.env.TARO_ENV` 编译期剔除，weapp 产物零残留，沿用原生导航）。
- **宣纸墨韵视觉系统**：设计令牌（CSS 变量）统一配色与圆角；卡片投影 + 标题装饰条；宗门概况卡（4 资源芯片 + 内外门/对方副行）；方针 4 分段控件；管理入口 3 列网格带箭头；按钮体系统一高度/圆角并适配 Taro H5（`disabled="false"` 反射、inline-block 收缩、`::after` 边框重置）。
- **文案与展示修复**：弟子列表/详情境界改显中文段位名（原为 `qi_refining_early` 枚举裸值）；对手页难度改显"简单/标准/困难 ×系数"。
- 验证：桌面 9:16（1280×800）与手机竖屏（390×844）双形态浏览器走查（开局/续档/推进月结/突破/六页导航/返回栏）；weapp 构建通过；`pnpm validate` 全绿（引擎 164 项 + store 14 项）。

## [0.2.1] - 2026-09-07

### 小程序（apps/miniapp）

- UI 全量接通（E05-F03 补收口）：八页面全部接通引擎——store 新增岗位/开炉/研读/穿戴/服丹/长老/宣战九个命令 action 与可读错误文案；丹房器坊（岗位任命、两丹开炉与在炉进度、装备炼制按宗门等级限档）；仓库三卡片 + 选人使用；对手宗门（名册伤势/宣战冷却/会战记录）；纪事页战报逐回合回放；弟子详情管理操作与伤势/buff 展示；主界面补对方宗门等级；结局页接甲乙丙丁评级与得分。
- store 单测 14 项（新增会战三战报回放与命令 round-trip 断言）；weapp 构建通过。

### Web/H5 基座与旅程验证（E05-F04）

- 按用户决策以 Web 栈为开发与验证基座：新增 `build:h5`/`dev:h5`（Taro H5，产物 `dist-web`）与 `scripts/web-serve.mjs` 本地伺服；存档经 localStorage 与 wx.setStorage 同构。
- 完整旅程在浏览器验证通过：开局三要素 → 推进/月结 → 历练六事件 → 弟子提拔/装备穿戴 → 丹房器坊岗位与开炉校验 → 仓库使用 → 宣战会战三战报逐回合回放 → 自动格/手动格存读一致（43,844 字节 <100KB）。weapp 构建保留，微信真机验证为可选项。

## [0.2.0] - 2026-09-07

简化版完整玩法收口：引擎 M1–M3 全量落地、千局调平达标、小程序 UI 纵切（部分）。

### 引擎（packages/engine）

- **战斗引擎（E01）**：`combat-profile` 属性派生（生命/物攻/法威/防御/先攻，玩家与 NPC 共用唯一公式）；`battle` 1v1 回合引擎（先攻定序、普攻与法术、命中 90%、暴击 1.5×、30 回合平局、战败伤势掷骰）；`battle-status` 状态效果（灼烧 6%/8%、冰冻、减速、护盾、反伤、状态抗性）。
- **内容目录与生产（E02）**：功法 8 门、法术 12 门（五行相性 +15%）、装备 4 槽 4 档固定数值、丹药 2 种；丹房/器坊点数生产（1 岗 +10 点/月，丹道/器道天赋 +20%）。
- **管理命令与历练（E02-F03/E03）**：研读功法法术、穿戴装备、服用丹药（聚灵丹 12 月 ×1.5 不叠加刷新）、任命长老（资源/战备）、提拔与岗位任命；历练六事件表（收获/切磋/掠夺/奇遇/危险/丹药奇缘）与伤势禁战、休养恢复 −2 月。
- **NPC 宗门与两宗对抗（E04）**：对手宗门生成与月度运行时（难度三档 ×0.8/1.0/1.2、装备按等级配档、自动升阶招募）；宣战与相遇接线（NPC 声望差 >50 或等级更高时 15%/月宣战）；3v3 会战（实力等级降序同序配对、1:1:1 比总伤害、掠 10% ≤5,000）；四结局（飞升/吞并/被吞并/凋敝连续 6 月）与甲乙丙丁仙途评级；大境界突破声望 +3。
- **数值调平（E06-F01）**：月真元基准常量 `ZHENYUAN_BASE` 10→130、悟性系数 0.1→0.5（唯一改动，公式与月结 9 步顺序不动）。1200 局模拟（三难度档 ×400，上限 600 游戏年）：飞升通关中位 35.9–36.9 游戏年（锚点 30–60 ✅）、吞并/被吞并 0%（≤25% ✅）、两宗等级差 ≤1 占比 0.94（✅）。报告：`scripts/balance-report.json`。
- **战报存档字节预算**：`state.battles` 保留最新 ≤30 条且序列化总量 ≤45KB，保障 120 月快照 <100KB（设计 D-022）。
- 测试：164 项引擎测试（含 golden 契约、同 seed 双跑 deepEqual、60 月长跑确定性）。

### 小程序（apps/miniapp）

- Taro 4.2.1（React）骨架、`wx.setStorage` 存档 4 格 round-trip（手动 3 + 自动 1）、1 秒 1 月自动推进定时器、首页（开局三要素 + 难度三选 + 存档管理）、主界面（概况栏 + 方针四选 + 推进/自动 + 月结弹层 + 升阶 + 存档）、弟子殿与弟子详情（五维/灵根/天赋/修炼视图/提拔）。
- weapp 构建通过；store 单测 11 项（round-trip、损坏容错、创建→推进→提拔→存读档等价）。
- 纪事页战报回放、丹房器坊/仓库/对手情报页命令接线、结局页评级展示已随 Unreleased 补齐（八页面全接通）。

### 已知未完成

- E05-F04：微信开发者工具导入运行与真机旅程验证（需用户本人操作；10 步清单见 `apps/miniapp/README.md`）。

## [0.1.0] - 2026-09-06

- 立项：从主仓迁出独立产品线；引擎 M1 月结闭环（境界 10 段/灵石经济/突破坐化/招募升阶/飞升结局），39 项切片测试。
- 权威设计定稿：`docs/design/2026-09-06-小程序简化版设计.md`（M2–M5 全表数值）。
